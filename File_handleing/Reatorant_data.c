#include <stdio.h>
int main()
{
  FILE *r;
  int i, id, num, qty, prize;
  char name[20], objact_name[20];
  float total;

  r = fopen("restorant.txt", "w");
  if (r == NULL)
  {
    printf("\nWriting file is not opening...");
  }
  else
  {
    for (i = 1; i < 2; i++)
    {
      
    }
  }
}